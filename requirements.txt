# Telegram
python-telegram-bot[job-queue]==21.10

# TTS (bundles espeak-ng; CPU-friendly VITS via onnxruntime)
piper-tts==1.8.0

# Database (HTTP client mode — server runs externally, e.g. Chroma Cloud)
# Verified combo: chromadb 0.5.23 breaks with pydantic>=2.10 (body deserialization)
chromadb==0.5.23
pydantic==2.9.2
fastapi==0.115.6

# HTTP
httpx==0.27.2
aiohttp==3.10.11

# SVG -> PNG preview rendering (needs libcairo2 in the container)
cairosvg==2.7.1

# Tests
pytest==8.3.4
pytest-asyncio==0.25.2
